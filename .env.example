# ============================================
# Supabase Configuration
# ============================================
# Get these values from your Supabase project settings:
# https://app.supabase.com → Your Project → Settings → API

# Project URL
# Standard cloud: https://xxxxxxxxxxxxx.supabase.co
# Local development: http://localhost:54321 (Supabase CLI)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Anon/Public Key (safe to expose in browser)
# Found in Settings → API → Project API keys → anon/public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# ⚠️ DANGER ZONE - Server-Only Keys
# ============================================
# These keys MUST NEVER be prefixed with NEXT_PUBLIC_
# They bypass Row Level Security and have full database access.

# Service Role Key (Optional for Phase 2.1, Required for Phase 2.2+)
# Found in Settings → API → Project API keys → service_role
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
